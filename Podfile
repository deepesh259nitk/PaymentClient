# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PaymentClient' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PaymentClient
  pod 'JOSESwift', '2.2.1', :inhibit_warnings => true
  pod 'CryptoSwift', '~> 1.3.1', :inhibit_warnings => true
  pod 'GMEllipticCurveCrypto', '1.3', :inhibit_warnings => true
  
  target 'PaymentClientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PaymentClientUITests' do
    # Pods for testing
  end

end
